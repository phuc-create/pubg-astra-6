{
  "name": "neon-strike",
  "version": "3.0.0",
  "private": true,
  "description": "NEON STRIKE FPS — phòng chơi và đấu đội thời gian thực",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server.js && node --check shared.js && node --check multiplayer.js"
  },
  "engines": { "node": ">=22" },
  "dependencies": { "ws": "^8.18.3" }
}
